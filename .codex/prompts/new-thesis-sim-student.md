---
description: Draft and create a new thesis-finder simulation student command
argument-hint: [student idea, field, response style, constraints, or full profile]
---

Use the `create-thesis-sim-student` skill.

Create a new repo-local thesis-finder simulation student from the user
arguments. First draft the complete student profile for review. Do not write
files until the user explicitly approves the profile.

User arguments:

```text
$ARGUMENTS
```

